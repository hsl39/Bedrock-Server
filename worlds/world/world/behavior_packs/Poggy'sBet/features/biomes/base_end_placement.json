{
	"format_version": "1.21.0",
	"minecraft:scatter_feature": {
		"description": {
			"identifier": "wiki:biomes/base_end_placement"
			//places a column for each column of blocks in the chunk
		},
		"places_feature": "wiki:biomes/end_block_picker",
		"iterations": "(math.abs(v.originx) > 200 || math.abs(v.originz) > 200) ? 0 : 256;",
		"x": {
			"distribution": "fixed_grid",
			"extent": [
				0,
				15
			]
		},
		"z": {
			"distribution": "fixed_grid",
			"extent": [
				0,
				15
			]
		},
		"y": 0
	}
}